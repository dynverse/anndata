# Read from a hierarchical Zarr array store.

Read from a hierarchical Zarr array store.

## Usage

``` r
read_zarr(store)
```

## Arguments

- store:

  The filename, a MutableMapping, or a Zarr storage class.

## Examples

``` r
if (FALSE) { # \dontrun{
ad <- read_zarr("...")
} # }
```
