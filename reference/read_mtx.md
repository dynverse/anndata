# read_mtx

Read `.mtx` file.

## Usage

``` r
read_mtx(filename, dtype = "float32")
```

## Arguments

- filename:

  The filename.

- dtype:

  Numpy data type.

## Examples

``` r
if (FALSE) { # \dontrun{
ad <- read_mtx("matrix.mtx")
} # }
```
